[Service]
ProtectHome=true
PrivateNetwork=false
ReadWritePaths=/var/lib/smolotchi /run/smolotchi
# Core is NET_ADMIN-free by default; only raw sockets are allowed.
AmbientCapabilities=CAP_NET_RAW
CapabilityBoundingSet=CAP_NET_RAW
